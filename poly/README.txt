The states polygons are directly converted form TIGER 2012 state boundaries
The US polygon is the result of a dissolve on those state polygons after applying a 0.1 degree buffer

Both files converted to poly format using ogr2poly.
